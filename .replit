language = "nix"
run = "yarn dev"

[nix]
channel = "unstable"

[env]
XDG_CONFIG_HOME = "/home/runner/.config"


[packager]
language = "nodejs"
ignoredPackages = ["vue", "#app", "#head", "vue-demi"]

  [packager.features]
  packageSearch = true
  guessImports = true
  enabledForHosting = false
